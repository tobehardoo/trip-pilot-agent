# P3.3 验收报告 · planner 策略节点（显式策略选择）

- 验收日期：2026-08-29
- 结论：**通过**
- 上游依据：[批次计划](批次计划.md)、[Agent化路线图](../../product/Agent化路线图.md) §4 P3.3

## 1. 交付内容

| 交付物 | 文件 | 说明 |
| --- | --- | --- |
| 策略契约 | `agent/graph.py` | `DECISION_SCHEMA` 增可选 `strategy` 枚举（DIRECT/RETRIEVE/CLARIFY/REPLAN/None，加法扩展）；非法值丢弃不猜 |
| 策略落盘 | `agent/graph.py` + `agent/state.py` | `Decision.strategy` → `AgentState.strategy` → checkpoint（轨迹可查）；`_decide_node` 每次决策记录 |
| 双路声明 | `agent/graph.py` | LLM 路径：prompt 指示声明策略；确定性路径（AskingDecider）：必填槽位缺失 → CLARIFY，齐备 → DIRECT |

## 2. 测试证据

- 脚本决策器声明的 RETRIEVE/CLARIFY 经 run_agent 落入末态（sink 断言）；确定性退路径产出 CLARIFY。
- 全量回归：**1866 passed / 42 skipped / 0 failed**（基线 1854 不降）；ruff 全绿。

## 3. 定位声明（记录在案）

本批交付的是**最小可观测形态**（策略进契约/状态/轨迹）；独立 V3 分叉拓扑是否值得，由 P3.4 回放 harness 的意图频率证据裁决——P2.5 的教训（无数据不立节点）延续适用。processor 不因此多发事件（克制）。
