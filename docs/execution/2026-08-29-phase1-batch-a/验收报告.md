# 批次 A 验收报告 · Phase 1 止血（P1.1–P1.5）

- 验收日期：2026-08-29
- 结论：**通过**
- 上游依据：[批次计划](批次计划.md)、[Agent化路线图](../../product/Agent化路线图.md) §2、ADR-015

## 1. 任务完成情况

| 任务 | 缺陷 | 结果 |
| --- | --- | --- |
| P1.1 修 D1：decide 捕获传输异常 | D1 | ✅ `StructuredOutputDecider.decide` 捕获 `httpx.HTTPError`（含超时）/`TimeoutError`/`ConnectionError`（含 OSError）→ 退化确定性策略（默认 `AskingDecider`，可注入 fallback）；unparsable 输出同样退化。run 以 WAITING_USER 收敛，不产生 FAILED |
| P1.2 修 D2：invoke 统一异常包装 | D2 | ✅ `ToolRegistry.invoke` 捕获 handler 异常 → `TOOL_ERROR` failure + 空 state 更新，run 继续 |
| P1.3 ask_user 支持 options / expected_type | — | ✅（随骨架批次已实现，本批补验收）非空字符串数组 ≤10、expected_type ∈ text/number/date/choice，非法参数 fail-closed（`INVALID_OPTIONS` / `INVALID_EXPECTED_TYPE`） |
| P1.4 修 D5：update_constraints 代码落槽 | D5 | ✅（随骨架批次已实现，本批补验收）confirmed 判定规则化 `rule:evidence-match`；legacy `confirmed` 标志被显式忽略 |
| P1.5 槽位状态机扩展 | — | ✅ `SlotState` 五态（UNKNOWN/INFERRED/CONFIRMED/REJECTED/USER_OVERRIDE）；`hard` = CONFIRMED ∪ USER_OVERRIDE；元数据 `verified_by` / `override_of` / `updated_at`；改值带审计链，同值提案被代码拒绝 |

## 2. 本轮修复的实现变更

- `agent/graph.py`：`decide` 双异常分支（unparsable / 传输失败），退化目标默认 `AskingDecider` 且可注入；docstring 同步。
- `agent/tools.py`：`invoke` 统一 `except Exception → TOOL_ERROR`；`_update_constraints` 的 `rejections` 现在记录模型提供的被拒值（此前被丢弃——空槽位上的拒绝无法阻止同值再提议）；docstring 同步。
- `agent/state.py`：`reject(name, *, value, evidence)` 支持显式被拒值（缺省沿用当前值）。
- 测试：修正 2 个 RED 前置条件（空槽位 reject/override 不携带被拒值/旧值的口径，改为先 fill 再 reject/override）；新增 `test_rejection_records_the_supplied_value_and_blocks_reproposal` 钉死 rejections 落值 + 同值再提议被拒语义。

## 3. 测试证据

- `pytest tests/agent -q`：**43 passed**（修复前 9 failed / 33 passed）。
- 全量回归（全新 `--basetemp`）：**1785 passed / 37 skipped / 0 failed**（9.3s）——批次前基线 1732 不降。
- `ruff check .`：All checks passed。

## 4. 门禁复核

- RED → GREEN：9 个先行 RED 测试全部转绿。✅
- 全量回归 + ruff：见上。✅
- 契约未动：无 schema_version 影响。✅
- 无 Key 环境：`test_asking_decider_still_runs_end_to_end_without_keys` 通过。✅

## 5. 遗留与说明

- D3（emit 暴露给 LLM）、D4（validate 守门对象是 slots 而非 itinerary）按路线图属 Phase 2（P2.3/P2.4）。当前缓解：emit 的一票否决（未过 validate 拒绝发出）已在位并有测试。
- 基线数字修订：1732 为批次开工前数字，本批后全量基线为 **1785 passed / 37 skipped**；后续批次以"每批开工时实测"的动态基线为准。
