# P2.8 验收报告 · 用户可见闭环

- **8a（Java 触发端点）：2026-08-29 完成**
- **8b（Web 对话页 · 旅程风）：2026-08-29 完成**
- 上游依据：[批次计划](批次计划.md)；视觉方案经用户四选一确定（A·Airbnb 旅程风）

## 1. 8a 交付内容

| 交付物 | 文件 | 说明 |
| --- | --- | --- |
| 触发端点 | `AgentDialogRunController`（新） | `POST /api/trips/{tripId}/agent-dialogue/runs`（消息 → AGENT_START）、`POST .../runs/{runId}/answers`（回答 → AGENT_RESUME），均 202 + `{eventId, status: "QUEUED"}` |
| 命令发布 | `AgentDialogCommandService`（新） | 构造 v1 契约完整信封 JSON（含 `userId` = P3.2 profile 身份；RESUME 含 `runId`）→ `OutboxEventRecord`（PENDING，routing key `agent.start`/`agent.resume`）——**事务性 outbox，零新发布机制**，既有 outbox job 负责投递 |
| 所有权守卫 | `TripOwnershipGuard`（新） | 函数式接缝 + `TripService` 适配器：非本人 trip → 404，不产生任何命令 |
| 幂等 | `Idempotency-Key` 头 | 可选，作为命令 eventId；主键冲突视为同键重放，返回同一 QUEUED 响应 |
| 校验 | service 层 | 消息/回答 1..2000 非空白（与契约 `pattern \S` 对齐），违规 400 且零写入 |

## 2. 8a 测试证据

- `AgentDialogCommandServiceTest` **6/6**：outbox 记录的路由键/eventType/信封字段（userId、runId、payload）与 Python pydantic 契约逐字段一致（JSON 往返断言）、所有权 404 零写入、非法消息 400 零写入、幂等重放同响应、outbox 写失败外露。
- **Java 全量套件 609 tests / 0 失败 —— BUILD SUCCESS**（基线 558 → 609，全程无回归；含 P2.7b/P2.8a 全部新组件与 Spring 装配验证）。
- Python 契约测试不受影响（1866 passed 基线不变）。

## 3. 8b 交付内容（Web 对话页 · 旅程风）

| 交付物 | 文件 | 说明 |
| --- | --- | --- |
| 设计 token | `tailwind.config.js` | primary 蓝改珊瑚 Rausch 系（#FF385C 主）、surface 冷灰改暖白沙墨阶；目的地默认渐变对齐珊瑚；radius/shadow/字体沿用现有体系——**token 层一次替换，全站换肤** |
| API 层 | `lib/api.ts` | `streamAgentDialogEvents`（复用 planning SSE 解析器 + Bearer + Last-Event-ID）、`startAgentRun` / `answerAgentRun`（Idempotency-Key 头） |
| 对话面板 | `AgentRunDialog.vue`（新） | 旅程风抽屉：用户珊瑚气泡 / AI 白卡 / **工具步等宽日志行（成败着色）** / 问题选项胶囊 / **行程卡片（渐变 Hero + 天次活动 + 应用按钮）**；SSE 重放 + 断线重连（连续 3 次失败熔断提示）；jsdom/旧环境无 WebCrypto 的幂等键降级 |
| 一键应用 | 同上 + `TripDetail` 接线 | COMPLETED 事件携带**确认槽位投影**（Python `_publish_completion` + 契约/fixture/Java parser 同步扩展）；前端按 evidence 语义只取 CONFIRMED、尽力类型转换（budget 数值化等）、未确认/不可转换跳过；应用后引导工作台「生成规划」 |
| 入口 | `TripDetail.vue` | 工作台头部「AI 助手」启动按钮 + 面板挂载 |
| 契约 | `agent-completed-event-v1` | payload 增可选 `slots`（Python 模型/Java record+parser/fixture 三侧同步，加法扩展） |

## 4. 8b 测试证据

- vitest：`AgentRunDialog.test.ts` **4/4**（START 命令与幂等键、问题渲染+选项回答、工具步+行程卡渲染、**应用映射**——CONFIRMED 才进约束、预算数值化、INFERRED 跳过）；**Web 全量 48 文件 499 passed**（基线 446 → 499）；`vue-tsc -b` 0 错误。
- Python 全量 **1866 passed / 42 skipped**（slots 投影进契约测试）；Java 全量 **609 tests / 0 失败**（slots parser 放行）。
- 过程修正：jsdom 无 `crypto.randomUUID`（幂等键降级）；组件必填 props 未传导致渲染为空（测试暴露）；契约测试同时暴露 `contracts.py` 缺 `Any` 导入。

## 5. 收官状态

**对话 → Agent 有界 run → 守门发射 → 事件落库 → SSE → 前端渲染 → 约束入 trip → 既有管线生成确定性行程**——用户可见闭环全链路贯通，三栈测试全绿（Python 1866 / Java 609 / Web 499）。v1.1 主线功能批次全部完成；Gate 2 评审材料就绪（P3.4 回放 harness + 5 场景不变量 + 本报告）。

## 4. 至此的后端链路状态

**用户 →（8a 触发端点）→ outbox → AMQP → agent.dialog.queue → 有界 run（build/validate/自动发射）→ 轨迹 + checkpoint 落库 → 三类事件 → Java 落库（幂等）→ SSE →（8b 前端订阅）**——除前端渲染外全部贯通并有测试背书。
