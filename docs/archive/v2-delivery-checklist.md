# V2.0 交付与验收清单

> 本清单是 V2.0 的执行与证据记录。除非对应的实现、测试和验收链接已补齐，所有条目保持未完成。
>
> 范围来源：[V2.0 方针与范围](v2-roadmap.md)。V1.3 历史证据见
> [V1.3 发布验收清单](release-checklist.md)。

## 使用方式

- 每个条目必须附上实现 PR/提交、自动化命令和可复现结果后才能勾选。
- 需要外部凭据、实际域名或第三方服务的项，记录为“外部阻塞”，不以 Demo 结果代替。
- 失败、降级和未完成项保留在清单中，不通过删除条目来缩小范围。
- 发生契约、迁移或对外行为变化时，同步更新相关实现文档。

## P0-0 V1.3 基线收口

- [ ] V2-P0-00 审查 V1.3 合并范围，确认无构建产物、临时代码、重复实现和失效 TODO。
- [ ] V2-P0-01 在干净工作区执行 Java、Python 和 Web 全量门禁，并记录实际版本和结果。
- [ ] V2-P0-02 在独立环境验证 Flyway V14-V22 的升级路径和消息契约。
- [ ] V2-P0-03 执行 Docker Compose 全栈健康与主流程验收。
- [ ] V2-P0-04 将经过验证的 V1.3 基线合并到主分支并创建稳定标签。
- [ ] V2-P0-05 更新 V1.3 发布证据；真实高德 Web JS Key、Security Code 与域名白名单单列为外部验收。

**证据记录**

| 日期 | 提交/标签 | 执行者 | 结果 | 外部阻塞 |
| --- | --- | --- | --- | --- |
| 待填 | 待填 | 待填 | 待填 | 待填 |

## P0-1 前端架构收敛

- [ ] V2-P0-10 引入 Vue Router，并实现登录、注册、旅行列表、创建、详情、规划、版本和分享路由。
- [ ] V2-P0-11 引入 Pinia，按认证、旅行、规划、行程、城市情报和通知划分状态所有权。
- [ ] V2-P0-12 将 `App.vue` 收敛为应用壳、会话初始化、全局通知、错误边界和路由出口。
- [ ] V2-P0-13 将创建旅行、规划任务、编辑、版本、城市情报和 SSE 连接编排迁移至 composable。
- [ ] V2-P0-14 保留请求竞态保护和 SSE `Last-Event-ID` 恢复，补充相应回归测试。
- [ ] V2-P0-15 统一 Dialog、空状态、错误状态、页面错误边界和关键骨架屏；清理无效依赖。
- [ ] V2-P0-16 验证路由刷新恢复、核心状态不经深层 props/emits 传递，且关键页面符合组件规模约束。

**执行检查点（2026-07-27，进行中）**

- 已在 Web 端接入 Vue Router 4.5.1 和 Pinia 3.0.3，注册了登录、注册、旅行、规划、版本和分享的目标路由名称；当前业务内容仍由兼容工作区承载，尚未达到逐页拆分的验收条件。
- `App.vue` 已收敛为 `RouterView`，应用入口安装 Router 与 Pinia；认证会话已迁移到 `auth` store。
- 路由切换统一按当前路由加载数据，并保留详情、攻略和行程请求的竞态保护。`pnpm --dir apps/web test` 于本检查点通过 17 个文件、84 项测试，`pnpm --dir apps/web typecheck` 通过。
- P0-1 的其余领域 store、composable、独立页面和统一状态组件仍未完成，因此本节条目保持未勾选。

## P0-2 真实规划进度

- [ ] V2-P0-20 在 `contracts/` 定义并校验版本化规划进度事件。
- [ ] V2-P0-21 事件包含 `stage`、`progress`、`message`、`occurredAt`、`taskId`、`sequence` 和可选统计。
- [ ] V2-P0-22 Worker 在接受任务、上下文、事实、召回、检索、排序、路由、求解、解释、持久化和完成阶段发送真实事件。
- [ ] V2-P0-23 Java 持久化关键进度，并通过 SSE 支持最后状态恢复和 `Last-Event-ID` 补发。
- [ ] V2-P0-24 重复或乱序事件不使进度倒退；取消和失败状态具有明确行为和用户可理解原因。
- [ ] V2-P0-25 Demo 模式复用同一协议；前端不再生成模拟百分比。
- [ ] V2-P0-26 采集每阶段耗时，并提供单元、集成和浏览器恢复测试。

**执行检查点（2026-07-27，进行中）**

- 已新增 `contracts/messaging/planning-progress-event-v1.schema.json`，并由 Python 模型、Java 解析器和 TypeScript API 类型共同校验。事件包含 `stage`、`sequence`、阶段边界 `progress`、`message`、`occurredAt`、任务标识和可选统计。
- Worker 在任务接收、上下文、城市事实、POI、排序、路线、约束、知识、解释和持久化的实际处理边界发布事件；Demo 和局部重规划只发布真实经历的阶段，前端会显示 `not used`，不会模拟百分比或步骤完成。
- Java 消费 `planning.progress.queue`，校验归属与递增序号，幂等持久化到现有任务事件流，并在首个进度事件将任务转为 `RUNNING`；现有 SSE 与 `Last-Event-ID` 回放机制复用该记录。
- 已验证 Java 152 项、Python 415 项（34 跳过）、Web 85 项、Web 类型检查和生产构建。阶段耗时指标和浏览器级 SSE 恢复仍未完成，因此 P0-2 保持未勾选。

参见 [规划进度契约](planning-progress.md)。

## P0-3 通勤数据写回

- [ ] V2-P0-30 为 `TransitLeg` 增加正式的活动起止、模式、距离、时长、费用、Provider、路线 ID、计算时间和过期标识。
- [ ] V2-P0-31 提供步行、公共交通、打车/驾车和自动推荐的影响预览。
- [ ] V2-P0-32 后端重算距离、时长、费用、出发时间、后续冲突、日交通时间、日预算和局部重规划需求。
- [ ] V2-P0-33 用户确认后创建不可变行程版本，版本差异显示通勤变化。
- [ ] V2-P0-34 Provider 失败时将估算或陈旧结果明确标记，并允许恢复后重新核验。
- [ ] V2-P0-35 覆盖确认、并发、版本、Provider 降级和冲突提示的自动化测试。

## P0-4 E2E 旅程

- [ ] V2-P0-40 新用户注册、创建广州三日旅行、规划、真实进度、地图和事实影响。
- [ ] V2-P0-41 北京攻略导入、城市情报同步与预约/天气事实进入结果。
- [ ] V2-P0-42 上海行程编辑、交通切换、局部重规划、差异比较和回滚。
- [ ] V2-P0-43 SSE 中断恢复、重复结果消息和 Java 幂等消费。
- [ ] V2-P0-44 E2E 使用独立测试数据库、受控 Demo Provider 和清理机制。
- [ ] V2-P0-45 四条核心旅程在 CI 中执行；真实 Provider 验收作为独立可选任务记录。

## P0-5 可观测性与诊断

- [ ] V2-P0-50 暴露规划创建、成功、失败、取消、平均/P95 耗时、阶段耗时和不可行比例。
- [ ] V2-P0-51 暴露 RabbitMQ 积压、消费延迟、重试、死信和重复消息指标。
- [ ] V2-P0-52 暴露 Provider 请求、成功、超时、限流、延迟、缓存命中和 Demo 降级指标。
- [ ] V2-P0-53 暴露城市情报有效/过期/冲突事实、刷新成功率和旧快照使用指标。
- [ ] V2-P0-54 关键日志贯穿 trace、旅行、任务、消息和 Provider 标识，并完成敏感字段脱敏测试。
- [ ] V2-P0-55 提供受保护诊断入口，支持查看失败任务、失败阶段、Provider 错误、重试和旧快照。
- [ ] V2-P0-56 对安全可重试任务实现幂等重试，并完成故障恢复演练。

## P1 使用与分发

- [ ] V2-P1-01 旅行列表后端分页、目的地/日期/状态筛选、最近修改排序、空状态和删除或归档。
- [ ] V2-P1-02 只读分享链接绑定不可变版本，支持创建、撤销、有效期和匿名只读。
- [ ] V2-P1-03 分享 Token 使用高熵随机值，数据库仅存摘要；完成限流、过期、撤销、所有权与版本隔离测试。
- [ ] V2-P1-04 生成可打印且不泄露内部字段的中文 PDF。
- [ ] V2-P1-05 生成时区、全日事件和定时事件正确的 `.ics` 文件。
- [ ] V2-P1-06 完成移动端只读行程、地图/路线、活动、地址复制、来源、交通和错误/加载体验。

## 发布门禁

- [ ] V2-REL-01 Java 单元/集成、Python 单元/集成、Web 单元/类型检查/构建通过。
- [ ] V2-REL-02 Flyway、JSON Schema、Java/Python/TypeScript 契约和示例消息校验通过。
- [ ] V2-REL-03 四条 E2E、Compose 全栈和 CI 门禁通过，核心测试没有静默跳过。
- [ ] V2-REL-04 备份与恢复、失败重试和 Provider 降级演练有记录。
- [ ] V2-REL-05 HTTPS 部署、Cookie 安全配置、Provider Key 隔离、分享链接安全与日志脱敏经过复核。
- [ ] V2-REL-06 文档只声明有证据的能力；路线图、架构、规划、API、数据库、部署和本清单一致。

## 发布证据模板

```text
版本/标签：
候选提交：
部署环境与日期：
Java：
Python：
Web：
迁移：
契约：
四条 E2E：
Compose：
备份恢复：
真实 Provider（可选）：
已知限制：
外部阻塞：
```
