# 批次 A 计划 · Phase 1 止血（P1.1–P1.5）

- 状态：已完成（2026-08-29 验收通过，见[验收报告](验收报告.md)）
- 日期：2026-08-29
- 上游依据：[Agent化路线图](../../product/Agent化路线图.md) §2 Phase 1、ADR-015
- 范围：**纯 Python 侧**（`src/trip_agent/agent/`），无契约变更、无 Java/Web 改动

## 1. 任务与修改点

| 任务 | 缺陷 | 文件 | 要点 |
| --- | --- | --- | --- |
| P1.1 | D1 | `agent/graph.py` | `StructuredOutputDecider.decide` 捕获 `httpx.HTTPError`（含超时）/`TimeoutError`/`ConnectionError` → 退化确定性策略（问第一个缺失必填槽位），run 不产生 FAILED |
| P1.2 | D2 | `agent/tools.py` | `ToolRegistry.invoke` 统一 `except Exception` → `TOOL_ERROR` failure，handler 异常转 ERROR observation |
| P1.3 | — | `agent/tools.py` + `agent/state.py` | `ask_user` 支持 `options`（非空字符串数组，≤10）与 `expected_type`（text/number/date/choice），非法参数 fail-closed；`AgentState` 增 `pending_options` / `pending_expected_type` |
| P1.4 | D5 | `agent/tools.py` | `update_constraints` 移除 LLM 自评 `confirmed`；confirmed 由规则判定：**用户原话（evidence）包含该值** 才落 CONFIRMED，否则 INFERRED |
| P1.5 | — | `agent/state.py` + `agent/tools.py` | 槽位状态机加 `REJECTED`（用户否定，同值提案被代码拒绝）与 `USER_OVERRIDE`（改值审计链，`override_of` 旧值）；`ConstraintSlot` 增元数据 `verified_by` / `override_of` / `updated_at`；`hard` = CONFIRMED ∪ USER_OVERRIDE |

## 2. 验收标准

- 新增测试全绿；Python 全量基线不降（批次前 1732，批次后 **1785 passed / 37 skipped**），ruff 0。
- 模型超时/HTTP 异常：run 正常收敛（WAITING_USER 或确定性退化），无异常冒泡。
- handler 抛异常：产生 `TOOL_ERROR` observation，run 继续。
- `confirmed=True` 不再存在；CONFIRMED 只能来自 evidence 值匹配（代码落槽）。
- 用户否定候选后，同值提案在代码层被拒绝；改值带 override_of 审计链。
- 无 Key 环境 AskingDecider 全链路可跑（回归覆盖）。

## 3. 门禁

- RED → GREEN → 全量回归 → 证据落盘（本目录验收报告）。
- pytest 必须使用全新 `--basetemp` 目录（历史坑：默认 Temp 权限不足 + basetemp 不可复用）。
- 契约未动，无 schema_version 影响。

## 4. 验收报告

见 [验收报告.md](验收报告.md)（完成后回填）。
