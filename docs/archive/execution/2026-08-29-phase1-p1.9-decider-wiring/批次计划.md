# 批次计划 · P1.9 决策器接线（STRUCTURED_MODEL_* → StructuredOutputDecider）

- 状态：已完成（2026-08-29 验收通过，见[验收报告](验收报告.md)）
- 日期：2026-08-29
- 上游依据：[Agent化路线图](../../product/Agent化路线图.md) §2 P1.9、[就绪审查报告](../2026-08-29-agent-readiness-review/审查报告.md) F4
- 说明：P1.9 原属批次 C，按审查结论提前执行（标准 3「动态决策」是"真 Agent"判据里缺口最高的一条）
- 范围：**纯 Python 侧**，无契约变更、无 Java/Web 改动、无新凭据面

## 1. 任务与修改点

| 任务 | 文件 | 要点 |
| --- | --- | --- |
| 决策配置对象 | `agent/factory.py`（新） | `DecisionModelConfig.from_env` 读共享 `STRUCTURED_MODEL_*` 面（ENDPOINT / API_KEY / NAME 三要素缺一即视为未配置；TIMEOUT_SECONDS 默认 8、MAX_INPUT_CHARACTERS 默认 30000，与 compose.prod.yaml 一致） |
| HTTP 决策传输 | `agent/factory.py` | `HttpDecisionTransport` 复用 guide-intelligence 的请求形态（HTTPS-only、Bearer、OpenAI-compatible `response_format: json_schema`、取 `choices[0].message.content`），system prompt 为决策语义，prompt 按 `MAX_INPUT_CHARACTERS` 截断，不记录密钥 |
| 生产构造点 | `agent/factory.py` | `build_decision_maker(tools, env, http_client)`：有配置 → `StructuredOutputDecider`；无配置 → `AskingDecider`（ADR-007 无 Key 可跑）。这是唯一 sanctioned 生产构造点，P2.1 入口接线必须经由它 |
| 导出 | `agent/__init__.py` | 导出 `build_decision_maker` / `DecisionModelConfig` |
| 测试 | `tests/agent/test_decider_factory.py` | 无 Key 退化 / 三要素不齐 fail-closed / 有 Key 全路径（MockTransport）决策 / HTTP 500 → D1 退化路径 / 非 HTTPS 拒绝 / prompt 截断 / 配置解析 |

## 2. 验收标准

- 无 Key 环境：`build_decision_maker` 返回 `AskingDecider`，`run_agent` 全链路 WAITING_USER 收敛。
- 有 Key（fake HTTP）：工厂产物走 `StructuredOutputDecider` → `HttpDecisionTransport` → 决策 JSON → `ToolCall`，请求携带共享凭据面（Bearer）与 `trip_pilot_agent_decision` schema 名。
- 传输 HTTP 错误经 D1 路径确定性退化，不冒泡。
- 三要素缺一 → `AskingDecider`（fail-closed，绝不半配置起跑）。
- 全量基线 **1785 passed / 37 skipped 不降**，ruff 0。

## 3. 门禁

- RED → GREEN → 全量回归 → 证据落盘（本目录验收报告）。
- pytest 使用全新 `--basetemp`。
- 契约未动，无 schema_version 影响；生产调用点（run 的触发入口）属 P2.1，不在本批。

## 4. 验收报告

见 [验收报告.md](验收报告.md)（完成后回填）。
