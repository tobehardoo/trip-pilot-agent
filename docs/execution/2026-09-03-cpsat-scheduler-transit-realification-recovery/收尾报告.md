# 收尾报告 — CP-SAT 调度内核 / manual-edit TRANSIT 真实化 / 僵死任务回收（P1–P3）

- 日期：2026-09-03
- 状态：已完成（提交 ea505e7 / 7015cb3 / aa6b547 / 9a8800a / 222e754，前置批次收口 G-1~G-6）
- 执行顺序：P0 存量批次收口 → P1 CP-SAT 日内调度 → P2 TRANSIT 真实化 → P3 任务恢复 → 全量回归

## P0 — 存量工作区收口（G-1 ~ G-6）

2026-09-02 composer 批次开发期间累积的 107 文件未提交改动（composer 工作台、
AUDIT-01/02/03 硬化、功能①/③、端点退场）按子系统分 6 笔提交收口，详见
[composer 批次收尾报告](../2026-09-02-composer-trip-creation/收尾报告.md)。
附带修复：功能③（默认日终 21:00）导致的 5 个测试失败（pace 基线 3→5、
松弛测试到达锚点钉 21:00 保持 B17 场景）；7 处活跃文档的 OR-Tools 不实表述。

## P1 — CP-SAT 日内调度内核（ea505e7…aa6b547）

- `planning/cpsat_schedule.py`：与贪心可行域一一镜像（容量折扣 / 节奏缓冲 /
  VERIFIED 营业窗口与闭馆 / must-include）的精确选择模型；求解失败一律回退贪心。
- 目标函数两层：选择层（score + 区域一致性 + 放置奖励，×1e5）+ 最早优先决胜层；
  score=0 候选仍被容量驱动放置（镜像贪心），空选择不会劣于填满。
- `PLANNING_DAY_SCHEDULER=GREEDY（默认）/CPSAT/SHADOW`，compose 与 .env.example 接线；
  ortools 依赖从「声明未用」转为真实接入（F-4.5 审计项闭环）。
- 验证：全量 2119 用例在 GREEDY 与 CPSAT 双模式同为全绿；SHADOW 模式全绿且零行为变化。
- 基准（`benchmarks/scheduler/`，10 确定性场景）：**CPSAT_BETTER 2 / TIE 8 /
  CPSAT_WORSE 0**，碎片化形态 +25%/+16.5% 总分，单日求解 ≤ 20ms；
  冒烟门禁钉住「CP-SAT 不劣于贪心」「可行域不漂移」两条不变量。

## P2 — manual-edit TRANSIT 真实化（9a8800a）

- 模式变更经既有内部端点 `/internal/v1/routes` 以真实路线刷新持久化快照
  （AMAP / estimated=false / 真实 polyline）；本地速度-成本估计函数退役。
- TAXI 复用 DRIVING 路线 + 规则计价（存储时长以等待时间兜底）；TRANSIT 需
  trip destination（city 经 TripMapper 贯通到 EditableItinerary）。
- Provider 不可用 → 502 `ITINERARY_TRANSIT_ROUTE_UNAVAILABLE`，不落伪造估计（fail-closed）。
- `simulateEdits` 工作视图不消耗 Provider 调用：旧 facts 标 `stale=true`，
  持久化路径统一刷新（AUTO 测试的 provider-call 预算语义保持）。

## P3 — planning 僵死任务回收器（222e754）

- 恢复分工：Worker 崩溃由 MQ 重投 + 完成幂等自愈；清扫器封住重投救不了的洞
  ——命令已 ack 但完成事件丢失/消息进死信 → 任务永久 RUNNING 并经
  one-active-per-trip 索引**锁死整趟旅行**。
- 超过 `app.planning.stale-task-timeout-minutes`（默认 30 分钟）的 QUEUED/RUNNING
  任务经 PlanningFailureService 常规失败路径回收：错误码 `STALE_TASK_REAPED`、
  终态事件落库（SSE/Last-Event-ID 闭环）、trip 阶段回滚、指标计入。
- 三问纪律取舍（记录在类 javadoc）：不做阶段级 checkpoint（重跑廉价且 Provider
  有 Redis 缓存）；RETRYING 归 Worker 重试/DLX 路径；WAITING_USER 是合法用户决策。

## 全量回归（最终状态）

| 栈 | 结果 |
| --- | --- |
| Python（agent-service） | **2079 passed / 42 skipped**，全仓 ruff 0 违规（G 批次遗留 14 处一并清零；GREEDY/CPSAT/SHADOW 三模式同绿） |
| Java（travel-server，JDK 21） | **616 passed / 0 failed**（含 Testcontainers 集成测试；P2 后 614、P3 后 616） |
| Web（apps/web） | typecheck 0 错；vitest **321 passed**；生产构建通过 |
| 调度器基准 | CPSAT_BETTER 2 / TIE 8 / CPSAT_WORSE 0，max solve 19.6ms |

## 已知债与后续

1. composer 链路的 e2e spec（`composer-creation.spec.ts`）仍未写（P0 报告在册）。
2. 24 个存量 mock e2e 的遗留红（归边/端点退场前契约）待按新契约重写。
3. CP-SAT 切主路径：建议先在 DEMO 环境以 SHADOW 跑一段真实流量，复核
   shadow 日志后把默认值切到 CPSAT（配置一行）。
4. 跨日联合优化（住宿锚定、跨天顺序）是 CP-SAT 内核的下一批。
5. RETRYING 任务的最终失败依赖 Worker 重试/DLX 语义，未纳入清扫器。
