# P1.9 验收报告 · 决策器接线（STRUCTURED_MODEL_* → StructuredOutputDecider）

- 验收日期：2026-08-29
- 结论：**通过**
- 上游依据：[批次计划](批次计划.md)、[Agent化路线图](../../product/Agent化路线图.md) §2 P1.9、[就绪审查报告](../2026-08-29-agent-readiness-review/审查报告.md) F4

## 1. 交付内容

| 交付物 | 文件 | 说明 |
| --- | --- | --- |
| 决策配置对象 | `agent/factory.py` | `DecisionModelConfig.from_env` 读共享 `STRUCTURED_MODEL_*` 面：ENDPOINT / API_KEY / NAME 三要素缺一即视为未配置（fail-closed，绝不半配置起跑）；TIMEOUT_SECONDS 默认 8、MAX_INPUT_CHARACTERS 默认 30000，与 compose.prod.yaml 一致；越界值（timeout ∉ (0,60]、输入 ∉ [1000,100000]）与明文 endpoint 显式 raise（配置错误必须响亮失败） |
| HTTP 决策传输 | `agent/factory.py` | `HttpDecisionTransport`：HTTPS-only、Bearer、OpenAI-compatible `response_format: json_schema`（schema 名 `trip_pilot_agent_decision`）、取 `choices[0].message.content`、prompt 按 `MAX_INPUT_CHARACTERS` 截断、不记录密钥——与 guide-intelligence 传输同一硬边界，system prompt 为决策语义 |
| 生产构造点 | `agent/factory.py` | `build_decision_maker(tools, env, http_client)`：有配置 → `StructuredOutputDecider`；无配置 → `AskingDecider`（ADR-007 无 Key 可跑）。唯一 sanctioned 构造点，P2.1 入口接线必须经由它（模块 docstring 已声明） |
| 包导出 | `agent/__init__.py` | `build_decision_maker` / `DecisionModelConfig` |
| 测试 | `tests/agent/test_decider_factory.py` | 9 个测试，httpx.MockTransport 覆盖配置解析 / fail-closed / 全路径决策 / 传输错误退化 / 截断 / HTTPS 拒绝 / 无 Key 全链路 |

## 2. 测试证据

- `pytest tests/agent -q`：**52 passed**（含 9 个新工厂测试）。
- 全量回归（全新 `--basetemp`）：**1794 passed / 37 skipped / 0 failed**（9.1s）——基线 1785 不降。
- `ruff check .`：All checks passed。

## 3. 门禁复核

- 无新凭据面：复用 `STRUCTURED_MODEL_*` 六个既有环境变量，未新增。✅
- 无 Key 全链路：`test_no_key_factory_feeds_a_converging_agent_loop`（WAITING_USER 收敛）。✅
- 有 Key 动态决策路径：`test_the_wired_decider_reaches_the_model_and_decides`（Bearer + schema 名 + 决策落 `ToolCall`）。✅
- 传输 HTTP 500 → D1 确定性退化路径，不冒泡。✅
- 契约未动：无 schema_version 影响。✅

## 4. 遗留与说明

- run 的生产触发入口（worker 分流 / HTTP 端点）属 P2.1，不在本批；届时调用点必须经由 `build_decision_maker`。
- `MAX_RETRIES` 环境变量未被决策路径消费：决策层不自行重试（每次 decide 一次调用，跨步重试由有界循环承担，上限 MAX_LLM_CALLS=8），与提取层的"有界重试"语义有意不同。
