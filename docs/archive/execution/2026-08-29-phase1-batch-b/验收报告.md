# 批次 B 验收报告 · Phase 1 可追踪（P1.6 轨迹落库 + P1.7 状态持久化）

- 验收日期：2026-08-29
- 结论：**通过**
- 上游依据：[批次计划](批次计划.md)、[Agent化路线图](../../product/Agent化路线图.md) §2 P1.6/P1.7、[就绪审查报告](../2026-08-29-agent-readiness-review/审查报告.md) F5

## 1. 交付内容

| 交付物 | 文件 | 说明 |
| --- | --- | --- |
| DDL | `agent/migrations/V1__create_agent_runs.sql` | `agent.agent_run`（status/stop_reason/answer/pending_question）、`agent.agent_step`（追加写轨迹，`(run_id, seq)` 唯一）、`agent.agent_checkpoint`（最新可恢复快照）；`command_event_id` 唯一部分索引 = P1.6 幂等键；全部落既有 `agent` schema，Java business schema 未碰 |
| 迁移器 | `agent/persistence.py` | 复用 acquisition 模式：checksum 版本化 SQL + `pg_advisory_xact_lock(742019, 13)`（与 acquisition 的 12 错开）+ `agent.agent_schema_migration` 登记表 |
| 仓储 | `agent/persistence.py` | `PsycopgAgentRunRepository`：`start_run`（command_event_id advisory lock + 查重，重复投递返回既有 run_id、created=False）、`record_step`（`ON CONFLICT (run_id, seq) DO NOTHING`，重投幂等）、`finish_run`（stop_reason→status 映射）、`save/load_checkpoint`、`load_run` |
| 序列化 | `agent/state.py` | `agent_state_to_dict` / `agent_state_from_dict`：version=1 标记、未知版本 fail-closed；五态槽位（含 verified_by/override_of/updated_at）、观测、pending 三件套、pending_call 全量往返；非 JSON 载荷降级 str（全量轨迹以 agent_step 为准，文档声明） |
| 流式钩子 | `agent/graph.py` | `run_agent(..., checkpoint_sink=...)`：astream（values 模式）逐节点累积状态回调 sink；无 sink 保持原 ainvoke 语义不变 |
| 编排门面 | `agent/persistence.py` | `AgentRunRecorder`：`start`（幂等面，重复命令返回既有 run）、`on_state`（新观测增量落 step + 每 tick checkpoint 快照）、`finish`（状态映射）；P2.1 入口三行接线 |
| 导出 | `agent/__init__.py` | 序列化函数、repository、recorder、`status_for_stop_reason` |

## 2. 测试证据

- `pytest tests/agent -q`：**62 passed / 4 skipped**（DB 门控）。
- 真实 PostgreSQL（一次性 `postgres:17-alpine` 容器 + `KNOWLEDGE_TEST_DATABASE_URL`）：`test_agent_persistence.py` **14 passed**——迁移幂等（连跑两遍）、start_run 同 command 幂等 / 异 command 各自成 run / 无 command 直建、record_step 重投幂等、checkpoint 保存/恢复往返、finish 落库 + load_run、以及全部单测（序列化往返、五态、sink 流式、recorder 增量与拒绝未 start）。
- 全量回归（无 DB 环境变量，基线口径）：**1804 passed / 41 skipped / 0 failed**——41 = 既有 37 + 本批 4 个 DB 门控；pass 口径 1794 不降。
- `ruff check .`：All checks passed。容器验收后销毁。

## 3. 门禁复核

- 契约未动，无 schema_version 影响。✅
- Java business schema 未碰（落既有 Python 侧 `agent` schema）。✅
- 无新依赖（psycopg 既有；Redis 后端按计划推迟到 P3.1）。✅
- LangGraph astream values 模式的逐节点全量语义由 sink 测试钉死（观测单调增长、末态与结果一致）。✅

## 4. 遗留与说明

- 生产调用点（AGENT_START 入口把 recorder 接进处理流程）归 P2.1；本批交付能力与唯一接线面（`AgentRunRecorder`）。
- Redis 后端推迟到 P3.1（可恢复中断需要 TTL 时引入），避免双后端无调用方空转；LangGraph 原生 checkpointer（含 pending 通道恢复）同批随 `interrupt()` 落地。
- agent_step 目前记录 `TOOL_OBSERVATION`（ok/summary/error_code）；决策与模型元数据（ADR-012 §4.3 的 prompt hash、token 数）随 P2.1 接入扩展 kind。
- WAITING_USER 期间并发消息语义（用户改口、第二条消息）在 P2.1 入口设计时定义，届时 command_event_id 之外的会话级约束一并落库。
